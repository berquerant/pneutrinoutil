version: '3'


tasks:
  default:
    desc: Start dev server
    deps:
      - install
    cmds:
      - pnpm run dev
  install:
    cmds:
      - CI=true pnpm i --ignore-scripts
  build:
    deps:
      - install
    cmds:
      - pnpm run build
  start:
    cmds:
      - pnpm run start
