version: '3'

includes:
  ui-dev:
    taskfile: ./DevTask.yml

tasks:
  default:
    deps:
      - swag
  swag:
    desc: Generate API
    deps:
      - task: ui-dev:install
    cmds:
      - ./bin/swag.sh
